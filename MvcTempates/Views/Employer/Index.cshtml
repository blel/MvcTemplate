@model IEnumerable<MvcTempates.Models.Employer>

@{
    ViewBag.Title = "Employers";
}

<h2>@ViewBag.Title</h2>



<!-- BLEL:Show table only if model contains any data. -->
@if (Model.Count() > 0){
    using (Html.BeginForm("Search","Employer")){
    <table>
        
        <tr>
            <th>
                Max. Records
            </th>
            <th style="text-align:left;">
                <input id="maxRecords" value="100" style="width:50px;"/>
            </th>
            
            <th style="text-align:right;">
                
                <input id="searchField" name="txt" />
                    
            </th>
            <th style="text-align:right;">
                <input type="submit" value="Search" />
                
            </th>
        </tr>
        <tr>  
            <td colspan="4">
    
<table class="table-normal">
    
    <tr>
        <th>
            @Html.DisplayName("Company Name")
        </th>
        <th>
            @Html.DisplayName("Street")
        </th>
        <th>
            @Html.DisplayName("Zip Code")
        </th>
        <th>
            @Html.DisplayName("Place")
        </th>
        <th>Actions</th>
    </tr>

@foreach (var item in Model) {
    <tr id ="@item.ID">
        <td>
            @Html.DisplayFor(modelItem => item.CompanyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Street)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ZipCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Place)
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table></td>
            </tr>
    </table> 
    
}
}
<p>
    @Html.ActionLink("Create New", "Create",null, new {@class = "button"})
</p>

<script type="text/javascript">
    //das scheint die art zu sein, wie in javascript ein link aufgerufen wird...
    //dass das mit this.id funktioniert, ist schon erstaunlich...
    $(".table-normal tr").click(function () {
        window.location.href = "/Employer/Edit/"+this.id;
    });
</script>